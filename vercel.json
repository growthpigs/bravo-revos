{
  "buildCommand": "node -e \"try{require('fs').writeFileSync('.env','')}catch(e){}\" && npm run build",
  "crons": [
    {
      "path": "/api/cron/health-check",
      "schedule": "0 8 * * *"
    }
  ]
}
