/**
 * /onboarding route - renders the Onboarding Hub
 * This exists because app/onboarding/start/ needs to be a subdirectory
 */

import CommandCenter from "../page"

export default function OnboardingPage() {
  return <CommandCenter />
}
